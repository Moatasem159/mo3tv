class AccountCustomListItem {
  String ?backdropPath;
  int ?id;
  String ?mediaType;
  String? originalTitle;
  String ?posterPath;
  String? title;
  String? name;
  String? originalName;
  AccountCustomListItem({
     this.backdropPath,
     this.id,
     this.mediaType,
     this.originalTitle,
     this.posterPath,
     this.title,
     this.name,
     this.originalName,
  });
}